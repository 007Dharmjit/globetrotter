import PageHeader from '../components/PageHeader'

export default function Dashboard() {
  return (
    <section>
      <PageHeader title="Dashboard" helper="Your trips at a glance — pick up where you left off or start something new." />
    </section>
  )
}
