import PageHeader from '../components/PageHeader'

export default function ActivitySearch() {
  return (
    <section>
      <PageHeader title="Explore activities" helper="Filter by category, cost and duration, then add to a stop." />
    </section>
  )
}
